export const getModel = jest.fn(() => ({
  generateText: jest.fn(),
  streamText: jest.fn(),
}));

export const getResponsesModel = jest.fn(() => ({
  generateText: jest.fn(),
  streamText: jest.fn(),
}));

